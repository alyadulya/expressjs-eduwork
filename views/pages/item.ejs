<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../templates/head', {title: item.name}); %>
</head>
<body>
    <header>
        <%- include('../templates/header') %>
    </header>
    <main class="container">
        <div class="row">
            <div class="col col-5">
                <img src=<%= "/image/" + item.image %> width="100%" alt="<%= item.name %>">
            </div>
            <div class="col">
                <h2><%= item.name %></h2>
                <h4 class="mb-4"><%= item.price %></h4>
                <p><%= item.description %></p>
                <a href="#" class="btn btn-primary" onClick="alert('still coming soon ...')">Add to cart</a>
                <a href="/products" class="btn btn-secondary">Go back</a>
            </div>
        </div>
    </main>
</body>
</html>